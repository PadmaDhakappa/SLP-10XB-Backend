services:
  - type: web
    name: slp-api
    env: python
    buildCommand: pip install -r app/requirements.txt
    startCommand: gunicorn app.main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        sync: false
        # Set this in Render dashboard: postgresql://neondb_owner:npg_l9v4WGDtRMzr@ep-***-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

